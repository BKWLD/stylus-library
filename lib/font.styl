/**
 * A base set of styles designed to be applied to containers
 * of database driven content that was populated through
 * CKEditor (or, convievably, another WYSIWYG editor)
 */
wysiwyg()

	// Add common margins to elements that may have been cleared
	ul, ol, p, h1, h2, h3, h4, h5, h6, blockquote
		margin-top 1em
		margin-bottom @margin-top

	// Don't add vertical margins if on the edges
	> *
		&:first-child
			margin-top 0 !important
		&:last-child
			margin-bottom 0 !important

	// Make bold work
	b, strong
		font-weight 700

	// Make italic work
	i, em
		font-style italic

	// Make lists work again
	// Based on http//stackoverflow.com/a/11737351/59160
	margin = 2.3em
	ul
		list-style-type disc
		list-style-position outside
		margin-left margin
		padding-left 0

	ol
		list-style-type decimal
		list-style-position outside
		margin-left margin
		padding-left 0

	ul ul, ol ul
		list-style-type circle
		list-style-position outside
		margin 0 auto 0 margin

	ol ol, ul ol
		list-style-type decimal // Another option might be "lower-latin"
		list-style-position outside
		margin 0 auto 0 margin

	// Blockquote styles
	// These are as generic as possible here. Any specific
	// blockquote styles will be in the project's sass
	blockquote
		margin-left margin
		margin-right @margin-left
		display block

	// images need a height: auto so they can maintain their aspect ratio if they
	// have their width set to 100%
	img
		height auto

	// Underline links
	a
		text-decoration underline
